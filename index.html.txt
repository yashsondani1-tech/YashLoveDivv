<!DOCTYPE html><html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Will You Be Mine? ❤️</title>
  <style>
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: 'Courier New', monospace;
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      color: #fff;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .terminal {
      width: 90%;
      max-width: 800px;
      background: #111;
      border-radius: 12px;
      box-shadow: 0 20px 50px rgba(0,0,0,0.6);
      overflow: hidden;
    }
    .header {
      background: #222;
      padding: 10px 15px;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .dot { width: 12px; height: 12px; border-radius: 50%; }
    .red { background: #ff5f56; }
    .yellow { background: #ffbd2e; }
    .green { background: #27c93f; }
    .content {
      padding: 25px;
      line-height: 1.6;
      min-height: 300px;
    }
    .line { opacity: 0; animation: fadeIn 0.6s forwards; }
    @keyframes fadeIn { to { opacity: 1; } }
    .question {
      margin-top: 30px;
      text-align: center;
    }
    button {
      margin: 15px;
      padding: 12px 24px;
      font-size: 16px;
      border: none;
      border-radius: 30px;
      cursor: pointer;
      transition: transform 0.2s, box-shadow 0.2s;
    }
    .yes {
      background: #ff4b7d;
      color: #fff;
      box-shadow: 0 10px 20px rgba(255,75,125,0.4);
    }
    .no {
      background: #555;
      color: #fff;
    }
    button:hover {
      transform: translateY(-2px);
      box-shadow: 0 15px 30px rgba(0,0,0,0.4);
    }
    .heart {
      font-size: 60px;
      animation: pulse 1s infinite;
      margin-top: 20px;
    }
    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.2); }
      100% { transform: scale(1); }
    }
  </style>
</head>
<body>
  <div class="terminal">
    <div class="header">
      <div class="dot red"></div>
      <div class="dot yellow"></div>
      <div class="dot green"></div>
    </div>
    <div class="content" id="content"></div>
  </div> <script>
    const content = document.getElementById('content');


    const lines = [
      'Initializing love.exe...',
      'Loading memories ❤',
      'Compiling smiles, laughs, late-night talks...',
      'No errors found.',
      '',
      'Dear Divya,',
      'In this chaotic world of bugs and exceptions,',
      'you are the only constant that makes my code run perfectly.',
      'You are my favorite function call.',
      'My safest loop.',
      'My forever algorithm.',
      ''
    ];


    lines.forEach((text, index) => {
      const p = document.createElement('p');
      p.textContent = text;
      p.className = 'line';
      p.style.animationDelay = `${index * 0.5}s`;
      content.appendChild(p);
    });


    setTimeout(() => {
      const question = document.createElement('div');
      question.className = 'question';
      question.innerHTML = `
        <h2>Divya, will you be my girlfriend? 💖</h2>
        <button class="yes" onclick="sayYes()">YES</button>
        <button class="no" onclick="moveNo(this)">NO</button>
      `;
      content.appendChild(question);
    }, lines.length * 500 + 500);


    function sayYes() {
      content.innerHTML = `
        <h2>Divya, you just made Yash the happiest person alive 😍</h2>
        <div class="heart">❤️</div>
        <p>Commit successful.</p>
        <p>Relationship status: <strong>Yash ❤️ Divya — FOREVER</strong></p>
      `;
    }


    function moveNo(btn) {
      const x = Math.random() * 200 - 100;
      const y = Math.random() * 200 - 100;
      btn.style.transform = `translate(${x}px, ${y}px)`;
    }
  </script></body>
</html>